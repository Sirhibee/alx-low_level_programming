#include <stdio.h>
/**
 * main - A program that prints the alphabet in lowercase
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	c = 'a';
	int num;
	while (num < 10)
	{
		(c <= 'z')
		printf("%d", num);
		num += 1;
	}
printf("\n");
return (0);
}
